<?php
    // include_once "dbConnection.php";
    // session_start();

    // $cv = $_SESSION["ucv"];
    // $em = $_SESSION["em"];

    // $sql = "SELECT * FROM billet where codeVoyage = '$cv' and e_mail = '$em';";
    // $sqlsdb = mysqli_query($conn,$sql);
    // $fetch = mysqli_fetch_array($sqlsdb);

    // $numBillet = $fetch['numBillet'];
    // $_SESSION['numbil'] = $numBillet;

    // $conn->close();